import React, { useState, useEffect } from "react";
import "../style/style.css";

function useButtonOn(buttonName) {
  const [buttonClicked, setButtonClicked] = useState(null);
  useEffect(() => {});
}
